import { NotFound } from "@/components/Illustrations";

const NotFoundPage = () => {
  return (
    <div className="grid h-full place-items-center">
      <NotFound>We didn&apos;t find what you were looking for.</NotFound>
    </div>
  );
};

export default NotFoundPage;
